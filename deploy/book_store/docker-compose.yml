services:
  book-api:
    image: ghcr.io/delunsss2345/book-store-backend:latest
    networks:
      - proxy-net
      - app-net

volumes:
networks:
  proxy-net:
    external: true
  app-net:
    external: true
