// Main Prisma schema entry point for the multi-file setup.
// Models and enums live in `prisma/models/*.prisma`.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}
